import styled from 'styled-components';

export const ContactsListLayout = styled.div`
  display: grid;
  margin: 0;
  grid-gap: 1vmin;
  grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
`;
